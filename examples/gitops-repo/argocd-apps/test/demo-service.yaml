apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: demo-service-test
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/example-org/gitops-repo.git
      targetRevision: main
      path: charts/demo-service
      helm:
        valueFiles:
          - $values/environments/test/demo-service-values.yaml
    - repoURL: https://github.com/example-org/gitops-repo.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: demo-test
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
